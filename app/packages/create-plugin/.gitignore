# 模板：templates/{default,worker,native-host,native} 直接入库（publish.mjs prepare 校验其存在）。
# 共享文档 templates/_shared/.agent 由 publish.mjs 从 ../../skills/dlient-plugin-dev 生成，
# 也一并入（生成命令：npm run prepare:templates）。
# 模板在包内测试构建时的 node_modules/dist/pack/.dlient 由下方通用规则覆盖；
# 仅保留对模板内 lockfile 的定向忽略。
templates/**/package-lock.json
*.tgz

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build outputs
node_modules
dist
dist-ssr
*.local

# dlient plugin local pack（包内测试模板时在 templates/* 下生成）
pack/
*.dlient
plugin.asar

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
